1. What is the difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll?

Ans:getElementById - Finds an element by just an ID. getElementsByClassName - Finds all elements with the same class name and returns an HTMLCollection. querySelector - Searches by CSS selector. Returns only the first match. querySelectorAll - Same as querySelector, but returns a NodeList of all matching elements.

2. How do you create and insert a new element into the DOM?

Ans:Create an element, and can type a document.createElement(), then I have to write in the brackets. What do I want to make an element, then I append the Child 

3. What is Event Bubbling? And how does it work?

Ans:It is a type of machine, imagine a card able  home section, then if I click the card  button, then the card goes  the interview section, this is a Event 

4. What is Event Delegation in JavaScript? Why is it useful?

Ans:The simple answer is the parent element to manage all child elements. It can be done through event delegation.

5. What is the difference between preventDefault() and stopPropagation() methods?

Ans:event.preventDefault() stops the browser's default action for an event, while event.stopPropagation() stops the event from ,

